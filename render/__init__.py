"""Zarr → pixels: palettes, single-frame PNG, MP4 stitching, and the
GL / torch device plumbing shared with the GPU compute stages.

See render/README.md for the module map and the colour philosophy.
"""
